cmake_minimum_required(VERSION 2.8)
project(NaturalColor)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(NaturalColor.out Natural_Color.cpp )
target_link_libraries(NaturalColor.out ${OpenCV_LIBS})
